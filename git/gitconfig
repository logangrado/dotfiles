[core]
    excludesfile = ~/.gitignore_global
    pager = delta

[interactive]
    diffFilter = delta --color-only

# [delta]
#     syntax-theme = Dracula
#     navigate = true
#     line-numbers = true
#     # Added lines: subtle dark green background, syntax fg
#     plus-style = "syntax #1e3a24"
#     # Removed lines: subtle dark red background, syntax fg
#     minus-style = "syntax #3a1e1e"
#     # Refined additions (word-level): slightly brighter green
#     plus-emph-style = "syntax #26613a bold"
#     # Refined removals (word-level): slightly brighter red
#     minus-emph-style = "syntax #613026 bold"
#     # Line number column colors
#     line-numbers-plus-style = "#50fa7b"
#     line-numbers-minus-style = "#ff5555"
#     line-numbers-zero-style = "#6272a4"

[user]
    name = Logan Grado
    email = grado.logan@gmail.com

[commit]
    template = ~/.gitmessage
        
[pager]
    branch = false
    log = less -iXFR                # Leave on screen after exit

[filter "lfs"]
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process

[log]
    date = format:"%y-%m-%d %H:%M"

[format]
   pretty = format:"%C(auto)%h%Creset%C(auto)%d%Creset %s %C(#505050)(%cd, %an)%Creset"
