;;; ../.dotfiles/doom.d/packages/ein.el -*- lexical-binding: t; -*-

(use-package! ein
  ;; :init
  ;; (map! :after ein
  ;;       :map ein:notebook--opened-map
  ;;       :localleader
  ;;       "c" #'ein:worksheet-execute-cell
  ;;   )
  )
