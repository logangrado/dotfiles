;;; ../.dotfiles/doom.d/packages/lsp-bridge.el -*- lexical-binding: t; -*-

;; LSP Bridge Mode: Some super fast auto-complete. Not sure if this works literally everywhere
;; https://github.com/manateelazycat/lsp-bridge
;; Make sure to install deps with `pip3'

;; (use-package! lsp-bridge
;;   :config
;;   (setq lsp-bridge-enable-log nil)
;;   (global-lsp-bridge-mode))
